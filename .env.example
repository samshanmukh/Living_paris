# Next.js frontend — Cloudflare Worker API URL (local dev default: http://localhost:8787)
API_URL=http://localhost:8787

# Public API URL for browser-side fetch (production example below)
NEXT_PUBLIC_API_URL=http://localhost:8787
# NEXT_PUBLIC_API_URL=https://living-paris-api.<your-subdomain>.workers.dev

# Optional — set on Cloudflare Worker via: wrangler secret put MAPBOX_ACCESS_TOKEN
# Enables turn-by-turn walking routes; without it POST /api/routes uses a Turf estimate
MAPBOX_ACCESS_TOKEN=

# Cloudflare Worker CORS (set in wrangler.toml [vars] or dashboard)
# ALLOWED_ORIGIN=http://localhost:3000
