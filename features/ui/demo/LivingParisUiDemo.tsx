"use client";

/**
 * @deprecated Use `/ui-sandbox` — full app with live map + API.
 * Kept for Lovable static component export (`uiDemoFixtures` only).
 */
export { uiDemoFixtures } from "@/features/ui/demo/fixtures";
